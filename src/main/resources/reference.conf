skyeng {
    db {
      url = "jdbc:postgresql://database:5432/"${POSTGRES_DB}
      user=${POSTGRES_USER}
      password=${POSTGRES_PASSWORD}
      driver="org.postgresql.Driver"
      connections = {
        poolSize = 10
      }
    }
    server {
      host="0.0.0.0"
      port=8080
    }
}
